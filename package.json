{
  "name": "js-algorithm",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "pretest": "flow && eslint .",
    "test": "jest --coverage",
    "precommit": "lint-staged && jest --coverage"
  },
  "lint-staged": {
    "*.js": [
      "eslint --fix",
      "git add"
    ],
    "*.{json,md}": [
      "prettier --write",
      "git add"
    ]
  },
  "author": "benliu723@gmail.com",
  "license": "ISC",
  "dependencies": {
    "typescript": "^4.2.3"
  },
  "devDependencies": {
    "husky": "^6.0.0",
    "jest": "^26.6.3",
    "lint-staged": "^10.5.4",
    "ts-jest": "^26.5.4",
    "tslint": "^6.1.3",
    "tslint-config-airbnb": "^5.11.2",
    "tslint-plugin-prettier": "^2.3.0"
  }
}
